# We use the input() function to collect data/information from user.
#Getting user name
name = input('What is your name?')
#Getting user age
age = input('What is your current age in years?')
# Getting user's location
location = input('Where do you currenty live?')

#Printing personalized message
print(f'Hello {name}, you are {age} years old and live in {location}.' )
